// import Cookies from "js-cookie";

const state = {
  formItem: {}, // 当前操作的表单item
  formList: [], // 表单元素集合
  formConfig: { // 表单属性配置
    labelWidth: 100,
    labelStatus: 'col',
    bgColor: null
  },
};

export default state
